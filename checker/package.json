{
  "name": "menu-image-checker-online",
  "version": "2.2.0",
  "private": true,
  "description": "Scanner menu/direct link dan gambar rusak dengan laporan HTML progresif untuk Render.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "scan": "node scan.js config.json",
    "check": "node --check server.js && node --check scan.js && node --check url-safety.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "playwright": "1.61.1"
  }
}
